"""Core module — replace with your own implementation."""

from __future__ import annotations


def add(a: int, b: int) -> int:
    """Return the sum of two integers.

    Args:
        a: First operand.
        b: Second operand.

    Returns:
        The sum of *a* and *b*.
    """
    return a + b
