<!-- platform-annex -->
# Platform notes

## Tool mapping

Dynamic context is collected by explicitly inspecting the repository files and
Git history before the workflow begins. The GitHub CLI is used for the same
repository operations on both supported hosts.

## Codex constraints

There is no functional degradation. If a separate PR workflow skill is not
available, follow this skill's checklist directly in the current session.
