<!-- platform-annex -->
# Platform notes

## Tool mapping

This skill uses filesystem inspection, ordinary user dialogue, and the
`vidprep` CLI. The same workflow is available on both supported hosts.

## Codex constraints

There is no functional degradation. The CLI confirmation step remains the
approval gate for applying the generated patch.
