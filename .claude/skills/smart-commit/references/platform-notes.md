<!-- platform-annex -->
# Platform notes

## Tool mapping

Dynamic context is collected by explicitly running the listed Git commands
before the workflow begins. No host-specific prompt substitution is required.

## Codex constraints

There is no functional degradation. Commit and push actions still require the
same explicit user authorization described in the workflow.
